# tf-imports.yaml — generated by generate-imports.sh on 2026-04-08T19:34:00Z
# Maps Pulumi resource name → provider import ID.
# Pass to pulumi up via: PULUMI_IMPORT_IDS_FILE=config/tf-imports.yaml pulumi up
#
# NOTE: Remove entries from this file once they have been successfully imported
# into Pulumi state. Leaving an import ID on an already-imported resource that
# needs replacement will cause Pulumi to error with "previously-imported
# resources that still specify an ID may not be replaced".

# Containers not yet in Pulumi state (exist on server, need import):
# All containers imported via pulumi import CLI. This file is empty.
