name: Pulumi Apply

on:
  push:
    branches:
      - main
    paths:
      - 'config/**'
      - 'pulumi/**'
  workflow_dispatch:

jobs:
  pulumi_apply:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Get secrets from Infisical
        id: ts-key
        env:
          INFISICAL_CLIENT_ID: ${{ secrets.INFISICAL_CLIENT_ID }}
          INFISICAL_CLIENT_SECRET: ${{ secrets.INFISICAL_CLIENT_SECRET }}
        run: |
          curl -1sLf 'https://dl.cloudsmith.io/public/infisical/infisical-cli/setup.deb.sh' | sudo -E bash
          sudo apt install -y infisical
          TOKEN=$(infisical login --method=universal-auth --client-id="$INFISICAL_CLIENT_ID" --client-secret="$INFISICAL_CLIENT_SECRET" --plain --silent)
          TS_KEY=$(INFISICAL_TOKEN="$TOKEN" infisical secrets get TAILSCALE_AUTH_KEY --plain --projectId "bdb8c859-f6b3-4cca-9e8d-dccc12607ff4" --env dev --path /server_access)
          PULUMI_TOKEN=$(INFISICAL_TOKEN="$TOKEN" infisical secrets get PULUMI_ACCESS_TOKEN --plain --projectId "bdb8c859-f6b3-4cca-9e8d-dccc12607ff4" --env dev --path /server_access)
          echo "::add-mask::$TS_KEY"
          echo "::add-mask::$PULUMI_TOKEN"
          echo "authkey=$TS_KEY" >> $GITHUB_OUTPUT
          echo "PULUMI_ACCESS_TOKEN=$PULUMI_TOKEN" >> $GITHUB_ENV

      - name: Connect to Tailscale
        uses: tailscale/github-action@v3
        with:
          authkey: ${{ steps.ts-key.outputs.authkey }}

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: pulumi/go.mod
          cache-dependency-path: pulumi/go.sum

      - name: Build custom providers
        working-directory: pulumi
        run: bash scripts/build-providers.sh

      - name: Install Pulumi CLI
        uses: pulumi/actions@v6

      - name: Run pulumi up
        working-directory: pulumi
        run: |
          pulumi up --stack ${{ secrets.PULUMI_STACK }} --yes --non-interactive
